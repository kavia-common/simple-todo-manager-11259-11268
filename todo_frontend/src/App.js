import React from 'react';
import TodoPage from './TodoPage';

// PUBLIC_INTERFACE
function App() {
  /** Application root which renders the TODO PAGE (Figma extracted). */
  return <TodoPage />;
}

export default App;
